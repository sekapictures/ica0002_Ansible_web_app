- name: Install nginx
  ansible.builtin.apt:
    name: nginx
    state: latest
    update_cache: yes

- name: Replace default HTML page
  ansible.builtin.copy:
    src: index.html
    dest: /var/www/html/index.nginx-debian.html
    force: yes   
